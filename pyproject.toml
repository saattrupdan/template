[project]
name = "machine-learning-repository-template"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "cookiecutter>=2.7.1",
    "pyyaml>=6.0.3",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "ruff>=0.16.6",
]
